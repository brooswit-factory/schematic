name: Release

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      version:
        description: "Pack version to build, e.g. 0.1.0 (for dry runs — no GitHub Release is created)"
        required: true
        type: string

jobs:
  release:
    uses: ./.github/workflows/_debug-called.yml
